import React from 'react'
import ReactDOM from 'react-dom/client'
import EnrollmentForm from './App.jsx' // Make sure this matches your file name
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <EnrollmentForm /> 
  </React.StrictMode>,
)